/**
 * =========================
 * GRADES
 * =========================
 */

model Grade {
  id         Int    @id @default(autoincrement())
  name       String @unique
  xpRequired Int

  users User[]
}
